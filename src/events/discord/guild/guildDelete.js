const { Guild, Client } = require("discord.js");
module.exports = {
    name: "guildDelete",
    /**
     * @param {Guild} guild
     * @param {Client} client
     */
    async execute(guild, client) {},
};
