const { Message, Client } = require("discord.js");
module.exports = {
    name: "messageDelete",
    /**
     * @param {Client} client
     * @param {Message} message
     */
    async execute(message, client) {},
};
